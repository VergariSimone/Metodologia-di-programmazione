public class ChristmasTree {

    public String toString(){
        String tree = "   /\\   \n";
        tree = tree + "  /  \\  \n";
        tree = tree + " /    \\ \n";
        tree = tree + "/      \\\n";
        tree = tree + "-------- \n";
        tree = tree + "  \"  \"  \n";
        tree = tree + "  \"  \"  \n";
        tree = tree + "  \"  \"  \n";
        return tree;
    }

}
